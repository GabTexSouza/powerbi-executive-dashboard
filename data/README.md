# Dados

A pasta `data/raw` será usada para armazenar a base original.

A pasta `data/processed` será usada para armazenar a base tratada que será carregada no Power BI.

Os arquivos de dados não serão versionados no GitHub.